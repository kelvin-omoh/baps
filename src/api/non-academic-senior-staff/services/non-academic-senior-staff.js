'use strict';

/**
 * non-academic-senior-staff service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::non-academic-senior-staff.non-academic-senior-staff');
