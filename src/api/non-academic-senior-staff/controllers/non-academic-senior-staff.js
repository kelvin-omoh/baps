'use strict';

/**
 * non-academic-senior-staff controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::non-academic-senior-staff.non-academic-senior-staff');
