'use strict';

/**
 * non-academic-senior-staff router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::non-academic-senior-staff.non-academic-senior-staff');
