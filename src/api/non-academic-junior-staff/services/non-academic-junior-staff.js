'use strict';

/**
 * non-academic-junior-staff service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::non-academic-junior-staff.non-academic-junior-staff');
