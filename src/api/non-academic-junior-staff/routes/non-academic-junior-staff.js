'use strict';

/**
 * non-academic-junior-staff router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::non-academic-junior-staff.non-academic-junior-staff');
