'use strict';

/**
 * non-academic-junior-staff controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::non-academic-junior-staff.non-academic-junior-staff');
